local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

-- Console Privacy
print = function() end
warn = function() end
error = function() end

local Window = Rayfield:CreateWindow({
   Name = "Metoird Hub | Universal Football",
   LoadingTitle = "Metoird Hub",
   LoadingSubtitle = "by metorid",
   ConfigurationSaving = {Enabled = true, FolderName = "MetoirdHub", FileName = "Config"},
   KeySystem = false
})

-- [[ VARIABLES ]]
local lp = game.Players.LocalPlayer
local char = lp.Character or lp.CharacterAdded:Wait()
local humanoid = char:WaitForChild("Humanoid")
local root = char:WaitForChild("HumanoidRootPart")

local currentBannerID = "rbxassetid://13181356130"
local originalBannerID = nil
local equippedAccessories = {}

-- [[ SETTINGS ]]
local hitboxEnabled = false
local hitboxSize = 2
local hitboxTransparency = 0.5 -- 🔧 CHANGE DEFAULT HERE (0 = visible, 1 = invisible)

local boostEnabled = false
local boostPower = 90

-- [[ TABS ]]
local CompTab = Window:CreateTab("Comp")
local EmotesTab = Window:CreateTab("Emotes")
local ItemTab = Window:CreateTab("Items")
local SpooferTab = Window:CreateTab("Spoofers")

-- [[ COMP TAB ]]
CompTab:CreateSection("--- Hitbox ---")

CompTab:CreateToggle({
    Name = "Hitbox Enabled",
    CurrentValue = false,
    Callback = function(V)
        hitboxEnabled = V
    end
})

CompTab:CreateSlider({
    Name = "Hitbox Size",
    Range = {1, 30},
    Increment = 1,
    CurrentValue = 2,
    Callback = function(V)
        hitboxSize = V
    end
})

-- ✅ NEW TRANSPARENCY SLIDER
CompTab:CreateSlider({
    Name = "Hitbox Transparency",
    Range = {0, 1},
    Increment = 0.05,
    CurrentValue = 0.5,
    Callback = function(V)
        hitboxTransparency = V
    end
})

CompTab:CreateSection("--- Boost Tech ---")

CompTab:CreateToggle({
    Name = "Enable Boost",
    CurrentValue = false,
    Callback = function(V)
        boostEnabled = V
    end
})

CompTab:CreateSlider({
    Name = "Boost Power",
    Range = {50, 250},
    Increment = 5,
    CurrentValue = 90,
    Callback = function(V)
        boostPower = V
    end
})

-- [[ EMOTES TAB ]]
EmotesTab:CreateSection("Animations")

local function PlayEmote(id, isLooped)
    for _, track in pairs(humanoid:GetPlayingAnimationTracks()) do
        track:Stop()
    end

    local anim = Instance.new("Animation")
    anim.AnimationId = "rbxassetid://" .. tostring(id)

    local load = humanoid:LoadAnimation(anim)
    load.Looped = isLooped
    load:Play()
end

EmotesTab:CreateButton({
    Name = "BBS",
    Callback = function()
        PlayEmote(2178463446, true)
    end
})

EmotesTab:CreateButton({
    Name = "Bop",
    Callback = function()
        PlayEmote(8028669437, true)
    end
})
EmotesTab:CreateButton({
    Name = "Hackers",
    Callback = function()
        PlayEmote(10714364213, true)
    end
})

EmotesTab:CreateButton({
    Name = "Stop All",
    Callback = function()
        for _, track in pairs(humanoid:GetPlayingAnimationTracks()) do
            track:Stop()
        end
    end
})

-- [[ ITEMS TAB ]]
ItemTab:CreateSection("Accessories")

local ACCESSORY_DATA = {
    AllStarGear = {
        mesh = "rbxassetid://13410505140",
        texture = "rbxassetid://13410505151",
        parent = "UpperTorso",
        scale = Vector3.new(1,1,1),
        offset = CFrame.new(0, 0, 0)
    },
    InfBandanna = {
        mesh = "rbxassetid://13540050100",
        texture = "rbxassetid://13540050115",
        parent = "Head",
        scale = Vector3.new(1.05,1.05,1.05),
        offset = CFrame.new(0, 0.1, 0)
    }
}

local function equipItem(name)
    if equippedAccessories[name] then return end

    local data = ACCESSORY_DATA[name]
    local p_part = lp.Character:FindFirstChild(data.parent)
    if not p_part then return end

    local part = Instance.new("Part", lp.Character)
    part.Name = name
    part.Size = Vector3.new(1,1,1)
    part.CanCollide = false
    part.Massless = true

    local mesh = Instance.new("SpecialMesh", part)
    mesh.MeshType = Enum.MeshType.FileMesh
    mesh.MeshId = data.mesh
    mesh.TextureId = data.texture
    mesh.Scale = data.scale

    local weld = Instance.new("Motor6D", p_part)
    weld.Part0 = p_part
    weld.Part1 = part
    weld.C0 = data.offset

    equippedAccessories[name] = part
end

for name, _ in pairs(ACCESSORY_DATA) do
    ItemTab:CreateToggle({
        Name = name,
        CurrentValue = false,
        Callback = function(v)
            if v then
                equipItem(name)
            else
                if equippedAccessories[name] then
                    equippedAccessories[name]:Destroy()
                    equippedAccessories[name] = nil
                end
            end
        end
    })
end

-- [[ SPOOFER TAB ]]
SpooferTab:CreateSection("Banners")

local bans = {
    ["TikTok"] = "14308515186",
    ["YT"] = "13181356130",
    ["Twitch"] = "13284240977",
    ["Global"] = "13223858189",
    ["50 Millon Rap"] = "13200847176",
    ["Staff"] = "13284239964"
   
}

for n, id in pairs(bans) do
    SpooferTab:CreateButton({
        Name = n,
        Callback = function()
            currentBannerID = "rbxassetid://" .. id
        end
    })
end

SpooferTab:CreateButton({
    Name = "Reset Banner",
    Callback = function()
        if originalBannerID then
            currentBannerID = originalBannerID
        end
    end
})

-- [[ HEARTBEAT LOOP ]]
game:GetService("RunService").Heartbeat:Connect(function()
    if not root or not lp.Character then return end

    if hitboxEnabled then
        for _, p in pairs(game.Players:GetPlayers()) do
            if p ~= lp and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                local hrp = p.Character.HumanoidRootPart

                hrp.Size = Vector3.new(hitboxSize, hitboxSize, hitboxSize)
                hrp.Transparency = hitboxTransparency

                -- optional visual
                hrp.Material = Enum.Material.ForceField
            end
        end
    end

    for _, v in pairs(lp.Character:GetDescendants()) do
        if v:IsA("ImageLabel") and v.Visible and v.AbsoluteSize.X > v.AbsoluteSize.Y then
            if not originalBannerID then
                originalBannerID = v.Image
            end
            v.Image = currentBannerID
        end
    end
end)

Rayfield:Notify({
    Title = "Metoird Hub",
    Content = "Loaded successfully (No Key System)!",
    Duration = 5
})
